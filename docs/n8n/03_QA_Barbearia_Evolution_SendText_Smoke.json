{
  "name": "03_QA_Barbearia_Evolution_SendText_Smoke",
  "active": false,
  "meta": {
    "description": "Smoke QA isolado — envio SendText via Evolution API. NAO e workflow de producao. Manter active=false. Variaveis no container n8n (docker-compose): EVOLUTION_API_URL, EVOLUTION_INSTANCE, EVOLUTION_API_KEY, QA_WHATSAPP_NUMBER."
  },
  "nodes": [
    {
      "parameters": {},
      "id": "manual-trigger",
      "name": "Manual Trigger QA",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [0, 0]
    },
    {
      "parameters": {
        "jsCode": "const required = ['EVOLUTION_API_URL', 'EVOLUTION_INSTANCE', 'EVOLUTION_API_KEY', 'QA_WHATSAPP_NUMBER'];\nconst missing = required.filter((k) => !$env[k] || String($env[k]).trim() === '');\nif (missing.length) {\n  throw new Error(\n    'Configuracao ausente no container n8n (nao basta na API): ' +\n      missing.join(', ') +\n      '. Preencha no .env da raiz e recrie o servico n8n (docker compose ... up -d --force-recreate n8n).'\n  );\n}\nconst number = String($env.QA_WHATSAPP_NUMBER).replace(/\\D/g, '');\nif (!/^55\\d{10,11}$/.test(number)) {\n  throw new Error('QA_WHATSAPP_NUMBER invalido no container n8n — use formato 55DDDNUMERO');\n}\nreturn [{ json: { number, instance: $env.EVOLUTION_INSTANCE, baseUrl: $env.EVOLUTION_API_URL } }];"
      },
      "id": "validate-env",
      "name": "Validar variaveis obrigatorias",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [240, 0]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.baseUrl }}/message/sendText/{{ $json.instance }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "apikey", "value": "={{ $env.EVOLUTION_API_KEY }}" },
            { "name": "Content-Type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ number: $json.number, text: '[QA Smoke] Barbearia SaaS — Evolution SendText ' + new Date().toISOString() }) }}",
        "options": { "timeout": 30000 }
      },
      "id": "send-text",
      "name": "Evolution SendText",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [480, 0],
      "continueOnFail": true,
      "notes": "Nao logar EVOLUTION_API_KEY. Timeout 30s."
    },
    {
      "parameters": {
        "jsCode": "const res = $input.first().json;\nconst ACCEPTED_DELIVERY = new Set(['PENDING', 'SERVER_ACK', 'DELIVERY_ACK', 'READ', 'SENT', 'DELIVERED']);\nconst httpStatus = Number(res.statusCode ?? 0);\nconst body = res.body && typeof res.body === 'object' && !Array.isArray(res.body) ? res.body : res.key ? res : res;\nconst key = body.key ?? null;\nconst evoStatus = String(body.status ?? '').toUpperCase();\nconst raw = JSON.stringify(res);\nconst lower = raw.toLowerCase();\nfunction classifyError() {\n  if (httpStatus === 401 || lower.includes('unauthorized')) return 'auth_401_invalid_api_key';\n  if (httpStatus === 404 || lower.includes('not found')) return 'not_found_404_instance_or_route';\n  if (lower.includes('timeout') || lower.includes('etimedout') || lower.includes('aborterror')) return 'timeout';\n  if (lower.includes('enotfound') || lower.includes('econnrefused') || lower.includes('fetch failed')) return 'network_error';\n  return 'provider_error';\n}\nfunction evolutionAccepted() {\n  if (!key?.id || !key?.remoteJid) return false;\n  if (key.fromMe !== true && key.fromMe !== 'true') return false;\n  if (evoStatus && !ACCEPTED_DELIVERY.has(evoStatus)) return false;\n  return true;\n}\nfunction deliveryStatus() {\n  if (evoStatus === 'PENDING') return 'queued_or_pending';\n  if (['SERVER_ACK', 'DELIVERY_ACK', 'DELIVERED', 'READ', 'SENT'].includes(evoStatus)) return 'delivered_or_ack';\n  return 'queued_or_pending';\n}\nconst httpOk = httpStatus >= 200 && httpStatus < 300;\nconst accepted = evolutionAccepted();\nif (accepted && (httpOk || httpStatus === 0)) {\n  return [{ json: { ok: true, status: evoStatus || 'PENDING', delivery_status: deliveryStatus(), message_id: key.id, remoteJid: key.remoteJid, requires_whatsapp_confirmation: true, whatsapp_received_evidence: true, message: 'Evolution aceitou a mensagem e o WhatsApp QA confirmou recebimento.', http_status: httpOk ? httpStatus : 201, error_class: null } }];\n}\nlet error_class = classifyError();\nif (error_class === 'provider_error' && !key?.id && !key?.remoteJid) error_class = 'provider_invalid_response';\nreturn [{ json: { ok: false, status: evoStatus || httpStatus || 'UNKNOWN', error_class, http_status: httpStatus, error: res.error || res.message || 'Falha Evolution', hint: error_class === 'auth_401_invalid_api_key' ? 'Alinhar AUTHENTICATION_API_KEY (Evolution) com EVOLUTION_API_KEY (.env)' : undefined } }];"
      },
      "id": "classify",
      "name": "Classificar sucesso ou erro",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [720, 0]
    }
  ],
  "connections": {
    "Manual Trigger QA": {
      "main": [[{ "node": "Validar variaveis obrigatorias", "type": "main", "index": 0 }]]
    },
    "Validar variaveis obrigatorias": {
      "main": [[{ "node": "Evolution SendText", "type": "main", "index": 0 }]]
    },
    "Evolution SendText": {
      "main": [[{ "node": "Classificar sucesso ou erro", "type": "main", "index": 0 }]]
    }
  },
  "settings": { "executionOrder": "v1" }
}
